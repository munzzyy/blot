{
  "name": "blot",
  "version": "0.1.0",
  "description": "A document redactor that destroys what it covers. Rasterize, ink, re-check: no text layer survives.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "e2e": "node test/e2e_app.mjs && node test/e2e_wrapper.mjs",
    "check": "bash tools/check-clean.sh"
  },
  "engines": {
    "node": ">=24"
  }
}
